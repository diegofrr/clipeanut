/* eslint-disable @typescript-eslint/no-explicit-any */
export default interface IStreamUploader {
  uploader: string;
  uploaderUrl: string;
  uploaderAvatar: string;
  uploaderVerified: boolean;
  uploaderSubscriberCount: number;
}
